{% extends 'base2.html.twig' %}

{% block title %}Neoprint{% endblock %}

{% block body %}
    <h1>Neoprint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ neoprint.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ neoprint.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ neoprint.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('neoprint_index') }}">back to list</a>

    <a href="{{ path('neoprint_edit', {'id': neoprint.id}) }}">edit</a>

    {{ include('neoprint/_delete_form.html.twig') }}
{% endblock %}
