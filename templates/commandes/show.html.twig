{% extends 'base2.html.twig' %}

{% block title %}Commandes{% endblock %}

{% block body %}
    <h1>Commandes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Nomprenoms</th>
                <td>{{ commande.Nomprenoms }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ commande.tel }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ commande.taille }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ commande.couleur }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ commande.texte }}</td>
            </tr>
            <tr>
                <th>Article</th>
                <td>{{ commande.article }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commande.commentaire }}</td>
            </tr>
            <tr>
                <th>Ladate</th>
                <td>{{ commande.ladate ? commande.ladate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ commande.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commandes_index') }}">back to list</a>

    <a href="{{ path('commandes_edit', {'id': commande.id}) }}">edit</a>

    {{ include('commandes/_delete_form.html.twig') }}
{% endblock %}
