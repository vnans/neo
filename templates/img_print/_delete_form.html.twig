<form method="post" action="{{ path('img_print_delete', {'id': img_print.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ img_print.id) }}">
    <button class="btn">Delete</button>
</form>
