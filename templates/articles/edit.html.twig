{% extends 'base2.html.twig' %}

{% block title %}Edit Articles{% endblock %}

{% block body %}
   <div class="row">
   	<div class="col-md-6">
        <img src=" {{ asset('img/articles/')}}{{ article.image }} " class="img-fluid" alt="">  
         <a href="{{ path('articles_index2') }}"> <button class="btn btn-primary" > Rétour</button></a>
      </div>
      <div class="col-md-6">
       {{ include('articles/_form.html.twig', {'button_label': 'Mettre à jour '}) }}
      </div>
   </div>

   {# <div class="row">
   	<div class="col-md-6">
   		<a href="{{ path('articles_index') }}">back to list</a>

    {{ include('articles/_delete_form.html.twig') }}
   	</div>
   	 
   </div> #}

   
{% endblock %}
