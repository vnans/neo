{% extends 'base2.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
 
  <div class="row">
      <div class="col-md-6">
        <img src=" {{ asset('img/articles/')}}{{ article.image }} " class="img-fluid" alt="">  
        <a href="{{ path('articles_index2') }}"> <button class="btn btn-primary" >Retour</button> </a>

  {#   <a href="{{ path('articles_edit', {'id': article.id}) }}">edit</a> #}

    {{ include('articles/_delete_form.html.twig') }}
      </div>
       <div class="col-md-6">
        <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ article.libelle }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ article.taille }}</td>
            </tr>
            <tr>
                <th>Collection</th>
                <td>{{ article.collection }}</td>
            </tr>
           {#  <tr>
                <th>Image</th>
                <td>{{ article.image }}</td>
            </tr> #}
        </tbody>
    </table>   
       </div>
  </div>   
  <div class="row">
     
    
 </div>  

    

{% endblock %}
