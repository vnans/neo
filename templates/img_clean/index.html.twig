{% extends 'base.html.twig' %}

{% block title %}ImgClean index{% endblock %}

{% block body %}
    <main id="main">

   <section id="featured-services">
      <div class="container">
        <div class="row">

          <div class="col-lg-4 box">
            <i></i>
          
          </div>

          <div class="col-lg-4 box box-bg">
            
           
          </div>

          <div class="col-lg-4 box">
           
          </div>

        </div>
      </div>
    </section><!-- #featured-services -->
  
    <!--==========================
      About Us Section
    ============================-->
     <section id="services">
      <div class="container">

        <header class="section-header wow fadeInUp">
          <h3>NEO CLEAN</h3>
          <h4>NETTOYAGE BUREAU</h4>
         
        </header>
          <div class="row about-cols">
            <p>Nous vous offrons nos services pour un cadre hygienique et tres sain. </p>

            
        </div>

      </div>
    </section><!-- #services -->

    <!--==========================
      Services Section
    ============================-->
    <section id="services">
      <div class="container">
          <div class="row about-cols">
  {% for img_clean in img_cleans %}
        <div class="col-md-4 wow fadeInUp">
            <div class="about-col">
              <div class="img">
                <img src="{{ img_clean.img }}" alt="" class="img-fluid">
                <div class="icon"></div>
              </div>
              <h2 class="title"><a href="#">{{ img_clean.titre }}</a></h2>
               <p>
 
              </p>
            </div>
          </div>
            
        {% else %}
            <tr>
                <td colspan="4">Aucune image enregistrée</td>
            </tr>
        {% endfor %}




        </div>


      </div>
    </section><!-- #services -->



      

 





   
 
   
  </main>

   {#  <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Img</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for img_clean in img_cleans %}
            <tr>
                <td>{{ img_clean.id }}</td>
                <td>{{ img_clean.titre }}</td>
                <td>{{ img_clean.img }}</td>
                <td>
                    <a href="{{ path('img_clean_show', {'id': img_clean.id}) }}">show</a>
                    <a href="{{ path('img_clean_edit', {'id': img_clean.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('img_clean_new') }}">Create new</a> #}
{% endblock %}
