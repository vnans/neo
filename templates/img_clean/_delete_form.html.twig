<form method="post" action="{{ path('img_clean_delete', {'id': img_clean.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ img_clean.id) }}">
    <button class="btn">Delete</button>
</form>
